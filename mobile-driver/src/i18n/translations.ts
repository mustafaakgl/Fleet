/** @deprecated Import from ./languages or use i18next via useTranslation(). */
export {
  DEFAULT_LOCALE,
  SUPPORTED_LOCALES,
  normalizeLocale,
  isSupportedLocale,
  LOCALE_TAGS,
  type AppLocale,
} from './languages';
