import apiClient from '../lib/api';

export const companiesService = {
  // Backend currently exposes no HTTP methods for this controller.
  // Keep this service as an integration placeholder.
  _api: apiClient,
};

export default companiesService;
