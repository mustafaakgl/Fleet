import apiClient from '../lib/api';

export const searchService = {
  // Backend currently has no dedicated search controller endpoint.
  // Keep this service as an integration placeholder.
  _api: apiClient,
};

export default searchService;
